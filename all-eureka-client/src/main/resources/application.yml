server:
  port: 8762

eureka:
  instance:
    hostname: localhost
  client:
#    registerWithEureka: false
#    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

spring:
  zipkin:
    base-url: http://localhost:9411
  application:
    name: all-eureka-client
